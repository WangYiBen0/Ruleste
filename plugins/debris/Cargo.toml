[package]
name = "ruleste-debris"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "FloatingDebris / foregroundDebris plugins — ambient bobbing rubble without atlas frames yet."
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
ruleste-plugin-api = { path = "../../crates/ruleste-plugin-api", features = ["plugin"] }
