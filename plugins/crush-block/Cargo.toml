[package]
name = "ruleste-plugin-crush-block"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "CrushBlock entity plugin — a riding platform that crushes when dashed into."
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
ruleste-plugins-api = { path = "../../crates/ruleste-plugins-api", features = ["plugin"] }
