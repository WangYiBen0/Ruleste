[package]
name = "ruleste-plugin-spikes"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Spikes entity plugin — the level's primary hazard, kills the player on contact."
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
ruleste-plugins-api = { path = "../../crates/ruleste-plugins-api", features = ["plugin"] }
