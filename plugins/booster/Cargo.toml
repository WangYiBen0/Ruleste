[package]
name = "ruleste-plugin-booster"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Booster entity plugin — the green/red dash pads that hurl the player onward."
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
ruleste-plugins-api = { path = "../../crates/ruleste-plugins-api", features = [
  "plugin",
] }
