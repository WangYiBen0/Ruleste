[package]
name = "ruleste-booster"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "Booster entity plugin — the green/red dash pads that hurl the player onward."
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
ruleste-plugin-api = { path = "../../crates/ruleste-plugin-api", features = ["plugin"] }
