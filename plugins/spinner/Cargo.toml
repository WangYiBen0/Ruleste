[package]
name = "ruleste-spinner"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "DustStaticSpinner hazard plugin — a rotating dust crystal that kills on contact."
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
ruleste-plugin-api = { path = "../../crates/ruleste-plugin-api", features = ["plugin"] }
