[package]
name = "ruleste-plugin-player"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Built-in player entity plugin for Ruleste."

[lib]
crate-type = ["cdylib"]

[dependencies]
ruleste-plugins-api = { path = "../../crates/ruleste-plugins-api", features = ["plugin"] }
