[package]
name = "ruleste-plugin-dream-block"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "DreamBlock entity plugin — a dark solid block with drifting particles and wobble edges."
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
ruleste-plugin-api = { path = "../../crates/ruleste-plugin-api", features = ["plugin"] }
