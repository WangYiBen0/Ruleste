[package]
name = "ruleste-plugin-rotate-spinner"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "RotateSpinner hazard plugin — an invisible spinner orbiting a node."
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
ruleste-plugins-api = { path = "../../crates/ruleste-plugins-api", features = ["plugin"] }
