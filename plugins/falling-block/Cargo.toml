[package]
name = "ruleste-plugin-falling-block"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "FallingBlock entity plugin — a Solid that crumbles away after the player stands on it."
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
ruleste-plugins-api = { path = "../../crates/ruleste-plugins-api", features = ["plugin"] }
