[package]
name = "ruleste-crushblock"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "CrushBlock entity plugin — a riding platform that crushes when dashed into."
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
ruleste-plugin-api = { path = "../../crates/ruleste-plugin-api", features = ["plugin"] }
